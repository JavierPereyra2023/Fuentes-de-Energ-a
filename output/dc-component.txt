<script type=\"text/x-dc\" data-dc-script=\"\" data-props=\"{&quot;background&quot;:{&quot;editor&quot;:&quot;enum&quot;,&quot;options&quot;:[&quot;negro&quot;,&quot;azul&quot;],&quot;default&quot;:&quot;negro&quot;,&quot;tsType&quot;:&quot;'negro' | 'azul'&quot;,&quot;section&quot;:&quot;Escena&quot;},&quot;autorotate&quot;:{&quot;editor&quot;:&quot;boolean&quot;,&quot;default&quot;:false,&quot;tsType&quot;:&quot;boolean&quot;,&quot;section&quot;:&quot;Escena&quot;}}\">\nconst SECTIONS = [\n  { id:'fairing', kicker:'Sección 01', title:'Cofia de carga', desc:'Estructura de fibra de carbono que protege el satélite durante el ascenso; se separa en dos mitades recuperables.',\n    rows:[['Altura','13,1 m'],['Diámetro','5,2 m'],['Material','Compuesto de carbono'],['Recuperable','Sí, en el mar']] },\n  { id:'stage2', kicker:'Sección 02', title:'Segunda etapa', desc:'Etapa superior que inserta la carga en su órbita final; puede reencenderse varias veces en el vacío.',\n    rows:[['Motores','1 × Merlin 1D Vacuum'],['Empuje','981 kN'],['Propelente','LOX / RP-1'],['Reencendidos','Múltiples']] },\n  { id:'interstage', kicker:'Sección 03', title:'Interetapa', desc:'Cilindro que une el núcleo central con la segunda etapa y aloja el sistema neumático de separación.',\n    rows:[['Función','Separación de etapas'],['Material','Carbono / aluminio'],['Sistema','Neumático']] },\n  { id:'core', kicker:'Sección 04', title:'Núcleo central', desc:'Primera etapa reforzada del Falcon Heavy; soporta las cargas de los dos propulsores laterales.',\n    rows:[['Diámetro','3,66 m'],['Motores','9 × Merlin 1D'],['Tanques','LOX / RP-1'],['Aterrizaje','Barcaza en el mar']] },\n  { id:'boosters', kicker:'Sección 05', title:'Propulsores laterales', desc:'Dos primeras etapas del Falcon 9 acopladas al núcleo; se separan a los ~2,5 min y regresan a tierra.',\n    rows:[['Cantidad','2'],['Motores','9 × Merlin 1D cada uno'],['Separación','≈ T+2 min 30 s'],['Aterrizaje','Zonas 1 y 2, Cabo Cañaveral']] },\n  { id:'engines', kicker:'Sección 06', title:'Octaweb · 27 Merlin', desc:'Configuración octaweb de nueve motores por núcleo: la mayor potencia operativa de un cohete en servicio.',\n    rows:[['Motores totales','27 × Merlin 1D'],['Empuje al despegue','22.819 kN'],['Empuje por motor','845 kN'],['Ciclo','Generador de gas']] },\n  { id:'gridfins', kicker:'Sección 07', title:'Aletas rejilla y patas', desc:'Cuatro aletas de titanio controlan el descenso hipersónico; las patas de fibra de carbono despliegan al aterrizar.',\n    rows:[['Aletas','4 por núcleo, titanio'],['Patas','4 por núcleo'],['Material patas','Carbono / aluminio'],['Función','Retorno y aterrizaje']] }\n];\n\nconst GENERAL = [\n  { k:'Altura', v:'70 m / 229,6 pies' },\n  { k:'Ancho', v:'12,2 m / 39,9 pies' },\n  { k:'Masa', v:'1.420.788 kg' },\n  { k:'Carga útil a LEO', v:'63.800 kg' },\n  { k:'Carga útil a GTO', v:'26.700 kg' },\n  { k:'Carga útil a Marte', v:'16.800 kg' }\n];\n\nclass Component extends DCLogic {\n  state = { hover: null };\n\n  renderVals() {\n    const s = SECTIONS.find(x => x.id === this.state.hover);\n    return {\n      rootRef: el => { this.rootEl = el; },\n      hostRef: el => { this.hostEl = el; },\n      labelRef: el => { this.labelEl = el; },\n      generalRef: el => { this.generalEl = el; },\n      sections: SECTIONS.map(x => ({ id:x.id, title:x.title })),\n      generalRows: GENERAL,\n      hasHover: !!s,\n      hoverKicker: s ? s.kicker : '',\n      hoverTitle: s ? s.title : '',\n      hoverDesc: s ? s.desc : '',\n      hoverRows: s ? s.rows.map(r => ({ k: r[0], v: r[1] })) : [],\n      onChipEnter: e => this.setHover(e.currentTarget.getAttribute('data-part')),\n      onChipLeave: () => this.setHover(null)\n    };\n  }\n\n  setHover(id, fromCanvas) {\n    if (this.state.hover !== id) this.setState({ hover: id });\n    this.applyHighlight(id);\n    if (this.labelEl) this.labelEl.style.opacity = (id && fromCanvas) ? '1' : '0';\n    if (this.generalEl) this.generalEl.style.opacity = id ? '0.18' : '1';\n  }\n\n  applyHighlight(id) {\n    if (!this.partMeshes) return;\n    Object.keys(this.partMeshes).forEach(key => {\n      const on = key === id;\n      this.partMeshes[key].forEach(m => {\n        if (!m.material.emissive) return;\n        m.material.emissive.setHex(on ? 0x2b4c74 : 0x000000);\n        m.material.emissiveIntensity = on ? 1 : 0;\n      });\n    });\n  }\n\n  componentDidUpdate(prevProps) {\n    if (prevProps.background !== this.props.background) this.applyBackground();\n    if (this.controls) this.controls.autorotate = this.props.autorotate === true;\n  }\n\n  applyBackground() {\n    if (!this.rootEl) return;\n    const blue = (this.props.background || 'negro') === 'azul';\n    this.rootEl.style.background = blue\n      ? 'radial-gradient(120% 90% at 62% 42%, #16283f 0%, #0a1524 45%, #040810 100%)'\n      : 'radial-gradient(120% 90% at 62% 42%, #14171c 0%, #0a0c10 45%, #05070a 100%)';\n  }\n\n  componentDidMount() {\n    this.applyBackground();\n    this.mounted = true;\n    if (this.mounted && typeof THREE !== 'undefined') this.init(THREE);\n  }\n\n  componentWillUnmount() {\n    this.mounted = false;\n    if (this.raf) cancelAnimationFrame(this.raf);\n    if (this.ro) this.ro.disconnect();\n    if (this.renderer) this.renderer.dispose();\n  }\n\n  init(THREE) {\n    const host = this.hostEl;\n    const w = host.clientWidth || 1200, h = host.clientHeight || 800;\n    const scene = new THREE.Scene();\n    const camera = new THREE.PerspectiveCamera(28, w / h, 0.5, 3000);\n    const renderer = new THREE.WebGLRenderer({ antialias:true, alpha:true });\n    renderer.setPixelRatio(Math.min(devicePixelRatio, 2));\n    renderer.setSize(w, h);\n    renderer.shadowMap.enabled = true;\n    renderer.shadowMap.type = THREE.PCFSoftShadowMap;\n    renderer.toneMapping = THREE.ACESFilmicToneMapping;\n    renderer.toneMappingExposure = 1.05;\n    host.appendChild(renderer.domElement);\n    this.renderer = renderer;\n\n    scene.add(new THREE.HemisphereLight(0x9fb6d4, 0x0a0c10, 0.55));\n    const key = new THREE.DirectionalLight(0xffffff, 2.4);\n    key.position.set(60, 110, 80);\n    key.castShadow = true;\n    key.shadow.mapSize.set(2048, 2048);\n    const cam = key.shadow.camera;\n    cam.left = -60; cam.right = 60; cam.top = 120; cam.bottom = -20; cam.near = 1; cam.far = 400;\n    scene.add(key);\n    const fill = new THREE.DirectionalLight(0x8fb6e8, 0.7); fill.position.set(-90, 40, 30); scene.add(fill);\n    const rim = new THREE.DirectionalLight(0xbcd4f2, 1.1); rim.position.set(-30, 60, -110); scene.add(rim);\n\n    const white = new THREE.MeshStandardMaterial({ color:0xf2f4f7, roughness:0.5, metalness:0.12 });\n    const cream = new THREE.MeshStandardMaterial({ color:0xe6e9ee, roughness:0.62, metalness:0.08 });\n    const dark = new THREE.MeshStandardMaterial({ color:0x14171c, roughness:0.65, metalness:0.35 });\n    const metal = new THREE.MeshStandardMaterial({ color:0x3a4048, roughness:0.35, metalness:0.85 });\n    const mats = [white, cream, dark, metal];\n\n    this.partMeshes = {}; SECTIONS.forEach(s => this.partMeshes[s.id] = []);\n    const rocket = new THREE.Group();\n    const add = (geo, baseMat, part, x, y, z, rx, rz) => {\n      const m = new THREE.Mesh(geo, baseMat.clone());\n      m.position.set(x, y, z);\n      if (rx) m.rotation.x = rx;\n      if (rz) m.rotation.z = rz;\n      m.castShadow = true; m.receiveShadow = true;\n      m.userData.part = part;\n      rocket.add(m);\n      this.partMeshes[part].push(m);\n      return m;\n    };\n    const cyl = (r1, r2, hh, seg) => new THREE.CylinderGeometry(r1, r2, hh, seg || 44, 1, false);\n    // ojiva de punta redondeada (los Falcon no terminan en punta afilada)\n    const ogive = (R0, H, bluntness) => {\n      const pts = [];\n      const steps = 30, tipR = R0 * (bluntness || 0.34);\n      const bodyH = H - tipR;\n      for (let i = 0; i <= steps; i++) {\n        const t = i / steps;\n        pts.push(new THREE.Vector2(tipR + (R0 - tipR) * Math.pow(Math.max(0, 1 - t * t), 0.62), bodyH * t));\n      }\n      for (let i = 1; i <= 10; i++) { // domo esférico del ápice\n        const a = (i / 10) * Math.PI / 2;\n        pts.push(new THREE.Vector2(tipR * Math.cos(a), bodyH + tipR * Math.sin(a)));\n      }\n      return new THREE.LatheGeometry(pts, 52);\n    };\n\n    const R = 1.83, BX = 3.95;\n    // núcleo central\n    add(cyl(R, R, 41), white, 'core', 0, 20.5, 0);\n    add(cyl(R + 0.02, R + 0.02, 6.2), cream, 'interstage', 0, 44.6, 0);\n    add(cyl(R, R, 10.4), white, 'stage2', 0, 52.9, 0);\n    // cofia\n    add(cyl(2.6, 2.15, 1.2), cream, 'fairing', 0, 58.7, 0);\n    add(cyl(2.6, 2.6, 7.4), white, 'fairing', 0, 63.0, 0);\n    const nose = new THREE.Mesh(ogive(2.6, 6.4, 0.30), white.clone());\n    nose.position.y = 66.7; nose.castShadow = true; nose.userData.part = 'fairing';\n    rocket.add(nose); this.partMeshes.fairing.push(nose);\n\n    // propulsores laterales + núcleo: motores, aletas, patas\n    const cores = [[0, 'core'], [-BX, 'boosters'], [BX, 'boosters']];\n    cores.forEach(([x, part]) => {\n      if (part === 'boosters') {\n        add(cyl(R, R, 41), white, 'boosters', x, 20.5, 0);\n        add(cyl(R, R * 0.94, 1.6), cream, 'boosters', x, 41.8, 0);\n        const bn = new THREE.Mesh(ogive(R * 0.94, 5.2, 0.26), white.clone());\n        bn.position.set(x, 42.6, 0); bn.castShadow = true; bn.userData.part = 'boosters';\n        rocket.add(bn); this.partMeshes.boosters.push(bn);\n      }\n      // octaweb\n      add(cyl(R + 0.06, R + 0.06, 3.4), dark, 'engines', x, 1.7, 0);\n      const nz = new THREE.CylinderGeometry(0.34, 0.62, 2.4, 22, 1, true);\n      const nzMat = new THREE.MeshStandardMaterial({ color:0x0e1116, roughness:0.45, metalness:0.6, side:THREE.DoubleSide });\n      for (let i = 0; i < 9; i++) {\n        const m = new THREE.Mesh(nz, nzMat.clone());\n        if (i === 0) m.position.set(x, -1.2, 0);\n        else {\n          const a = (i - 1) / 8 * Math.PI * 2;\n          m.position.set(x + Math.cos(a) * 1.18, -1.2, Math.sin(a) * 1.18);\n        }\n        m.castShadow = true; m.userData.part = 'engines';\n        rocket.add(m); this.partMeshes.engines.push(m);\n      }\n      // aletas rejilla\n      [[-1, 0], [1, 0], [0, -1], [0, 1]].forEach(([dx, dz]) => {\n        const fin = new THREE.Mesh(new THREE.BoxGeometry(dx ? 2.2 : 2.6, 0.22, dz ? 2.2 : 2.6), dark.clone());\n        fin.position.set(x + dx * (R + 1.0), part === 'boosters' ? 39.6 : 39.2, dz * (R + 1.0));\n        fin.castShadow = true; fin.userData.part = 'gridfins';\n        rocket.add(fin); this.partMeshes.gridfins.push(fin);\n      });\n      // patas\n      [[-1, -1], [1, -1], [-1, 1], [1, 1]].forEach(([dx, dz]) => {\n        const leg = new THREE.Mesh(cyl(0.16, 0.3, 5.2, 14), dark.clone());\n        leg.position.set(x + dx * 1.35, 1.6, dz * 1.35);\n        leg.rotation.set(dz * 0.28, 0, -dx * 0.28);\n        leg.castShadow = true; leg.userData.part = 'gridfins';\n        rocket.add(leg); this.partMeshes.gridfins.push(leg);\n      });\n    });\n\n    // rótulo vertical \"FALCON HEAVY\" en el núcleo central\n    const c = document.createElement('canvas');\n    c.width = 256; c.height = 1024;\n    const g2 = c.getContext('2d');\n    g2.clearRect(0, 0, 256, 1024);\n    g2.translate(128, 512); g2.rotate(-Math.PI / 2);\n    g2.fillStyle = '#0d1014';\n    g2.font = '600 128px \"Barlow Condensed\", Helvetica, sans-serif';\n    g2.textAlign = 'center'; g2.textBaseline = 'middle';\n    g2.letterSpacing = '14px';\n    g2.fillText('FALCON HEAVY', 0, 0);\n    const tex = new THREE.CanvasTexture(c);\n    tex.anisotropy = 8;\n    const decal = new THREE.Mesh(\n      new THREE.CylinderGeometry(R + 0.015, R + 0.015, 13.5, 48, 1, true, -0.34, 0.68),\n      new THREE.MeshBasicMaterial({ map:tex, transparent:true, side:THREE.DoubleSide, depthWrite:false })\n    );\n    decal.position.set(0, 30, 0);\n    decal.userData.part = 'core';\n    rocket.add(decal); this.partMeshes.core.push(decal);\n\n    scene.add(rocket);\n    this.rocket = rocket;\n\n    const ground = new THREE.Mesh(new THREE.CircleGeometry(150, 64), new THREE.ShadowMaterial({ opacity:0.55 }));\n    ground.rotation.x = -Math.PI / 2; ground.position.y = -1.9; ground.receiveShadow = true;\n    scene.add(ground);\n\n    const starGeo = new THREE.BufferGeometry();\n    const pos = new Float32Array(700 * 3);\n    for (let i = 0; i < 700; i++) {\n      const r = 500 + Math.random() * 600, t = Math.random() * Math.PI * 2, p = Math.acos(2 * Math.random() - 1);\n      pos[i * 3] = r * Math.sin(p) * Math.cos(t); pos[i * 3 + 1] = r * Math.cos(p); pos[i * 3 + 2] = r * Math.sin(p) * Math.sin(t);\n    }\n    starGeo.setAttribute('position', new THREE.BufferAttribute(pos, 3));\n    scene.add(new THREE.Points(starGeo, new THREE.PointsMaterial({ color:0x9fb2c8, size:2.4, sizeAttenuation:false, transparent:true, opacity:0.55 })));\n\n    // órbita manual\n    const ctl = this.controls = { theta:0.6, phi:1.28, radius:180, target:new THREE.Vector3(0, 33, 0), dragging:false, autorotate: this.props.autorotate === true };\n    const el = renderer.domElement;\n    el.style.touchAction = 'none';\n    let px = 0, py = 0;\n    const ray = new THREE.Raycaster(), ndc = new THREE.Vector2();\n    const pick = ev => {\n      const r = el.getBoundingClientRect();\n      ndc.x = ((ev.clientX - r.left) / r.width) * 2 - 1;\n      ndc.y = -((ev.clientY - r.top) / r.height) * 2 + 1;\n      ray.setFromCamera(ndc, camera);\n      const hit = ray.intersectObjects(rocket.children, false)[0];\n      if (this.labelEl) this.labelEl.style.transform = `translate(${ev.clientX - r.left}px, ${ev.clientY - r.top}px)`;\n      this.setHover(hit ? hit.object.userData.part : null, true);\n    };\n    el.addEventListener('pointerdown', ev => { ctl.dragging = true; px = ev.clientX; py = ev.clientY; el.setPointerCapture(ev.pointerId); if (this.rootEl) this.rootEl.style.cursor = 'grabbing'; });\n    el.addEventListener('pointerup', ev => { ctl.dragging = false; if (this.rootEl) this.rootEl.style.cursor = 'grab'; });\n    el.addEventListener('pointermove', ev => {\n      if (ctl.dragging) {\n        ctl.theta -= (ev.clientX - px) * 0.006;\n        ctl.phi = Math.max(0.35, Math.min(2.3, ctl.phi - (ev.clientY - py) * 0.005));\n        px = ev.clientX; py = ev.clientY;\n      } else pick(ev);\n    });\n    el.addEventListener('pointerleave', () => this.setHover(null));\n    el.addEventListener('wheel', ev => { ev.preventDefault(); ctl.radius = Math.max(60, Math.min(420, ctl.radius * (1 + ev.deltaY * 0.0012))); }, { passive:false });\n\n    this.ro = new ResizeObserver(() => {\n      const nw = host.clientWidth, nh = host.clientHeight;\n      if (!nw || !nh) return;\n      camera.aspect = nw / nh; camera.updateProjectionMatrix(); renderer.setSize(nw, nh);\n    });\n    this.ro.observe(host);\n\n    const tick = () => {\n      this.raf = requestAnimationFrame(tick);\n      if (ctl.autorotate && !ctl.dragging && !this.state.hover) ctl.theta += 0.0016;\n      camera.position.set(\n        ctl.target.x + ctl.radius * Math.sin(ctl.phi) * Math.sin(ctl.theta),\n        ctl.target.y + ctl.radius * Math.cos(ctl.phi),\n        ctl.target.z + ctl.radius * Math.sin(ctl.phi) * Math.cos(ctl.theta)\n      );\n      camera.lookAt(ctl.target);\n      renderer.render(scene, camera);\n    };\n    tick();\n  }\n}\n<\u002Fscript>\n\n\n<\u002Fbody><\u002Fhtml>"
  